var cards = {};

//Use this space to prepare mapping metadata for application

cards.mapping = (type,values) => {

  // var subquery = {
  //   "ChargeCode" : ` and derived_charge_code in (${values})`,    
  // }
  // return subquery[type]

}

module.exports = cards;
